const fs = require('fs'); let f = fs.readFileSync('src/app/booking/page.tsx', 'utf8'); f = f.replace(/setRooms\\(Math\\.max\\(1, Number\\(paramRooms\\)\\)\\);/, 'setRoomSelections(prev => { const activeType = (Object.keys(prev)).find(k => prev[k] > 0) || \'deluxe2\'; return { ...prev, [activeType]: Math.max(1, Number(paramRooms)) }; });'); fs.writeFileSync('src/app/booking/page.tsx', f);
